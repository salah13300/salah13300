# Base de données (dev : SQLite local, prod : remplacer par une URL PostgreSQL — voir README section "Stack")
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_SECRET="change-me-in-production"
NEXTAUTH_URL="http://localhost:3000"

# --- Prestataires externes à brancher avant tout lancement public (cahier des charges section 9) ---
# KYC (ex: Sumsub, Veriff, Yoti, Onfido) — actuellement mocké, voir src/lib/kyc.ts
KYC_PROVIDER="mock"
KYC_API_KEY=""

# Paiement high-risk/adult (ex: CCBill, Segpay, Epoch) — actuellement mocké, voir src/lib/payments.ts
PAYMENT_PROVIDER="mock"
PAYMENT_API_KEY=""
